{# {% extends 'base.html.twig' %}

{% block favIcon %}
     <link rel="shortcut icon" href="assets/img/ico/fav.png">
 {% endblock %}

{% block body %}
    <h1>JobOffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_offer.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_offer.description }}</td>
            </tr>
            <tr>
                <th>JobTitle</th>
                <td>{{ job_offer.jobTitle }}</td>
            </tr>
            <tr>
                <th>Salary</th>
                <td>{{ job_offer.salary }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ job_offer.notes }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ job_offer.location }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ job_offer.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ job_offer.createdAt ? job_offer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ job_offer.updatedAt ? job_offer.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_job_offer_index') }}">back to list</a>

    <a href="{{ path('app_job_offer_edit', {'id': job_offer.id}) }}">edit</a> #}

{#   
{% endblock %}

{% block javascriptFooter %}


<script type="text/javascript">

    onAppReady(function() {
        $('.preload').remove();
    });

    
</script>
{% endblock %} #} 







{% extends 'base.html.twig' %}

{% block favIcon %}
     <link rel="shortcut icon" href="assets/img/ico/fav.png">
 {% endblock %}

{% block body %}

   <!-- Page Header-->
        <section class="page-title page-title-bg fixed-bg overlay dark-5 padding-top-160 padding-bottom-80">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <h2 class="white-text">{{ job_offer.jobCategory.name }}</h2>
                        {# <span class="white-text">Ref. 1007</span> #}
                        <ol class="breadcrumb">
                            <li>
                                <div class="portfolio-nav">
                                    <a href="#!" class="waves-effect waves-dark"><i class="fa fa-angle-left" aria-hidden="true"></i> Prev</a>
                                    <a href="#!" class="waves-effect waves-dark"><i class="fa fa-th-large" aria-hidden="true"></i></a>
                                    <a href="#!" class="waves-effect waves-dark">Next <i class="fa fa-angle-right" aria-hidden="true"></i></a>
                                </div>
                            </li>
                        </ol>
                    </div>
                </div>
            </div>
        </section>

        <!-- Page Content-->
        <section class="single-project-section section-padding light-gray-bg">
            <div class="container">
                <div class="project-overview">
                    <div class="row mb-80">
                        <div class="col-xs-12 col-md-8">
                            <p class="ref grey-text no-margin">Ref. {{ job_offer.reference }}</p>
                            <h2>{{ job_offer.jobTitle }}</h2>
                           
                            <p>
                            {{ job_offer.description }}
                            </p>
                        </div>

                        <div class="col-xs-12 col-md-4 quick-overview">
                            <ul class="portfolio-meta">
                           
                                <li><span> Pulished at </span>{{ job_offer.createdAt.date }}</li>
                                <li><span> Position </span>2nd Engineer</li>
                                <li><span> Contract Type </span>{{ job_offer.jobType.name }}</li>
                                <li><span> Salary </span>{{ job_offer.salary }}k &euro;</li>
                                <li><span> Location </span>{{ job_offer.location }}</li>
                                {# <li><span> Starting date </span>2018-05-18</li> #}
                            </ul>
                             {% set hasApplied = false %}
                                        {% for candidature in app.user.candidate.Applications %}
                                            {% if candidature.jobOffer.id == job_offer.id %}
                                                {% set hasApplied = true %}
                                                
                                            {% endif %}
                                        {% endfor %}
                                        
                                         {% if hasApplied %}
                                         <div class="btn btn-block btn-success mt-30 waves-effect waves-light disabled">You have applied for this job</div>
                                            {% else %}
                            <a class="btn btn-block gradient primary mt-30 waves-effect waves-light" href="{{ path('app_application_new', {id: job_offer.id} ) }}">Apply for this job</a>
                            {% endif %}
                            
                        </div>
                    </div>
                </div>

                <nav class="single-post-navigation no-margin" role="navigation">
                    <div class="row">

                        <div class="col-xs-6 col-sm-6 col-md-4">
                            <div class="previous-post-link">
                                <a class="btn border primary waves-effect waves-dark" href="#!">
                                    <i class="fa fa-long-arrow-left"></i>Previous
                                </a>
                            </div>
                        </div>
                        <div class="hidden-xs hidden-sm col-md-4"></div>


                        <div class="col-xs-6 col-sm-6 col-md-4">
                            <div class="next-post-link">
                                <a class="btn border primary waves-effect waves-dark" href="#!">
                                    Next <i class="fa fa-long-arrow-right"></i>
                                </a>
                            </div>
                        </div>

                    </div>
                </nav>
            </div>
        </section>

{% endblock %}

{% block javascriptFoote %}


<script type="text/javascript">

    onAppReady(function() {
        $('.preload').remove();
    });
</script>
{% endblock %}
